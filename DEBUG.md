# Как проверить ошибки расширения Chrome

## 1. Проверка ошибок в Popup (всплывающем окне)

1. Откройте расширение (кликните на иконку расширения)
2. **Правой кнопкой мыши** на всплывающем окне → **"Проверить"** (Inspect)
3. Откроется DevTools с консолью
4. Смотрите вкладку **Console** - там будут все ошибки и логи

## 2. Проверка ошибок в Background Script (фоновый скрипт)

1. Откройте `chrome://extensions/`
2. Найдите ваше расширение "Shorts Limiter"
3. Нажмите на **"service worker"** или **"background page"** (синяя ссылка)
4. Откроется DevTools для фонового скрипта
5. Смотрите вкладку **Console** для ошибок

## 3. Проверка ошибок в Content Script (скрипт на странице YouTube)

1. Откройте страницу YouTube
2. Нажмите **F12** или **Правой кнопкой → "Проверить"**
3. В DevTools перейдите на вкладку **Console**
4. Фильтруйте логи по имени расширения или ищите ошибки

## 4. Проверка Storage (хранилище данных)

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Application** (или **Хранилище**)
3. В левом меню найдите **Local Storage** → `chrome-extension://[ID_расширения]`
4. Или используйте **Storage** в DevTools расширения

## 5. Быстрая проверка через chrome://extensions/

1. Откройте `chrome://extensions/`
2. Включите **"Режим разработчика"** (Developer mode) в правом верхнем углу
3. Найдите ваше расширение
4. Нажмите **"Ошибки"** (Errors) - если есть ошибки, они будут показаны

## Полезные команды для отладки

В консоли popup или background можно выполнить:

```javascript
// Проверить текущую статистику
chrome.storage.local.get(['stats', 'userSettings'], console.log);

// Очистить статистику
chrome.storage.local.set({ stats: { dailyTime: 0, lastUpdated: Date.now(), history: [] } });

// Проверить активные сессии
// (только в background script)
```

## Типичные проблемы

1. **Прогресс-бар не обновляется:**
   - Проверьте консоль popup на ошибки
   - Убедитесь, что `updateProgressBar` вызывается
   - Проверьте, что данные в storage обновляются
   - **Тест прогресс-бара:** В консоли popup выполните:
     ```javascript
     // Проверить текущие данные
     chrome.storage.local.get(['stats'], r => console.log(r));
     
     // Тест прогресс-бара с 50%
     document.getElementById('progressBar').style.width = '50%';
     document.getElementById('progressBar').style.background = 'linear-gradient(90deg, #FFA500, #FF6347)';
     ```

2. **Время не отслеживается (всегда показывает 0):**
   - Откройте YouTube Shorts и начните смотреть видео
   - Проверьте консоль content script на странице YouTube (F12)
   - Должны быть логи: "🎬 Начали отслеживание Shorts"
   - Проверьте background script - должны быть логи: "📊 Обновлена статистика"
   - **Проверка данных:**
     ```javascript
     // В консоли popup
     chrome.storage.local.get(['stats'], (r) => {
         console.log('Stats:', r.stats);
         console.log('Daily time:', r.stats?.dailyTime || 0, 'seconds');
     });
     ```

3. **Прогресс-бар показывает 0%:**
   - Это нормально, если вы еще не смотрели Shorts
   - Откройте YouTube Shorts и посмотрите несколько видео
   - Подождите 5-10 секунд и откройте popup снова
   - Прогресс-бар должен обновиться

4. **Ошибки TrustedScriptURL:**
   - Это нормально, они подавляются автоматически
   - Не влияют на работу расширения

## Быстрая диагностика

1. **Проверка отслеживания:**
   - Откройте YouTube Shorts
   - Откройте консоль (F12) на странице YouTube
   - Должны быть логи: "🚀 Shorts Limiter запущен", "🎬 Начали отслеживание"

2. **Проверка сохранения данных:**
   - Посмотрите Shorts 10-20 секунд
   - Откройте popup расширения
   - В консоли popup должно быть: "Current stats in storage" с dailyTime > 0

3. **Проверка отображения:**
   - Если dailyTime > 0, но прогресс-бар не виден
   - Проверьте в консоли: "Progress bar computed width" - должно совпадать с процентом

